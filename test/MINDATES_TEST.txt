/* test of declare schema : we redefine the test schemas for merge
	compare results with MERGE_TEST_B if you want   :-)
 */
// this shall pass
declareSchema DateSchema = [ {KEY : int }; {D1 : date}; {D2 : date}; {D3 : date}; {D4 : date} ]

//declareSchema TestSchemaY = [ {EXTKEY : int }; {ATTRIBUTEY : string}; {ATTRIBUTEY2 : string} ]

INPUT1 (FileInputModule)
parameters
      inputFileName = DATEMIN_TEST.csv
      inputSchema   = DateSchema
end

MINAPPLIED (ApplyFunction)
inputs
	DateSchema = INPUT1
parameters
	function = MIN( D1, D2, D3 ) 
	result   = D4
end

MINDATES_OUTPUT (FileOutputModule)
inputs 
	DateSchema = MINAPPLIED
parameters
	checksum = none
	compression = none
end


