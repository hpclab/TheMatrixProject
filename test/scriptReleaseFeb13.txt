HOSP (ScriptInputModule)
parameters
    scriptFilename = LoadHOSP.txt
    inputName = HOSP
    expectedSchema = IADhosp
end

FFirstDiabetesDischarge (FilterModule)
inputs
    IADhosp = HOSP
parameters
    conditions = [{MAIN_DIAGNOSIS  matches "250*"}]
end

/* this is commented out -- if we want to have it, insert a sort module before
// test
// sceglie il primo ricovero per diabete
First_Diabetes_Discharge (AggregateModule)
inputs
  IADhosp = FFirstDiabetesDischarge
parameters
  isInputSorted = false
  groupBy =  [ PATIENT_ID ]
  functions = [ MIN( END_DATE ) ]
 results =  [ { DateFirstDiabetesDischarge : date } ]
end
*/